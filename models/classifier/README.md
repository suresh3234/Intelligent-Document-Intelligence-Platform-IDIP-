# Document Classifier Module

2-stage classifier ensemble (XGBoost + BERT CLS token) classifying documents into 8 classes with probability calibration (temperature scaling) and confidence threshold checks.
